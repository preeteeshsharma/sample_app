<% provide(:title, 'All users') %>
<h1>All users</h1>
<%= will_paginate %>
<ul class="users">
  	<%= render @users %>
	
	<% if false %>
		<% @users.each do |user| %>
    		<%= render user %>
  		<% end %>
	<% end %>
	
<%= will_paginate %>
</ul>